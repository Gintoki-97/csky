//
// Created by Gintoki-97 on 2019-05-05.
//

#include <stdio.h>
#include <stdlib.h>

#include "a.seqlist.cpp"

/**
 * Tag: [Z-2010]
 *
 * Question:
 * - 设将 n (n > 1) 个整数存放到一位数组 R 中；设计一个在时间和空间都尽可能搞笑的算法，
 *   将 R 中保存的序列循环左移 p (0 < p < n) 个位置，即将 R 中的数据由 (x0, x1, x2
 *   , ..., xn) 变换为 (xp, xp+1, xp+2, ..., xn, x0, x1, ..., xp-1)；
 *
 * Requirement:
 * - 给出算法的基本设计；
 * - 根据算法的设计思想，使用任意编程语言描述算法，关键之处给出注释；
 * - 说明算法的时间复杂度和空间复杂度；
 **/